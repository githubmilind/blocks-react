import React from 'react';
import ReactDOM from 'react-dom';

import BlockList from './components/block_list';
import MenuNav from './components/menu_nav'

// Take this component's generated HTML & Put it on the page / in the DOM
//ReactDOM.render(<BlockList />, document.querySelector('.container'));
ReactDOM.render(<MenuNav />, document.querySelector('.container'));
